# 本地密钥文件 —— 复制为 secrets.yaml 后填写
# load_secrets() 启动时把这里的 key 注入到环境变量（不会覆盖已 export 的同名变量）
#
# 真实文件 config/secrets.yaml 已在 .gitignore 中，不会被提交。

# Tavily Search API（可选，启用 type=tavily 的数据源时需要）
# TAVILY_API_KEY: tvly-xxxxxxxxxxxxxxxxxxxx

# 邮件 SMTP 授权码（启用邮件通知时需要）
# EMAIL_PASSWORD: your-smtp-app-password

# 飞书自定义机器人 webhook（启用飞书通知时需要）
# FEISHU_WEBHOOK: https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxx

# 企业微信群机器人 webhook（启用企业微信推送时需要）
# 获取：企业微信群 → 群设置 → 群机器人 → 添加 → 自定义机器人 → 复制 URL
# WECOM_WEBHOOK: https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxxxxxxx
